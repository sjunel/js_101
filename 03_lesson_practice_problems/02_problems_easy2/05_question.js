// Given an array of numbers, mutate the array by removing
// the number at index 2

let numArr = [1, 2, 3, 4, 5];


// Given an array
// Remove an element in the middle of the ordered list

numArr.splice(2, 1);
console.log(numArr);