// write out pseudocode (both casual and formal) that does the following:
// a function that takes an array of integers,
// and returns a new array with every other element


// Given an array of integers.
// Declare a function that accepts one parameter.
// Iterate through each array element.
// Select every even-index element, add value to a new array.
// Return the new array.

// START
// # Given an array of integers
// SET function with one parameter
// SET newArray to an empty array
// WHILE the number of iterations is <= to the number of array elements
//   IF the index of the element is even
//     Add element to newArray
//   ELSE
//     Skip element
// PRINT newArray
// END